{
  "name": "@pashki/platform-client",
  "version": "0.1.0",
  "private": true,
  "description": "The seam. Auth, family, entitlements and quota — the only code permitted to touch platform tables.",
  "type": "module",
  "exports": {
    ".": {
      "types": "./src/index.ts",
      "default": "./src/index.ts"
    },
    "./crypto": {
      "types": "./src/crypto.ts",
      "default": "./src/crypto.ts"
    },
    "./supabase": {
      "types": "./src/supabase-store.ts",
      "default": "./src/supabase-store.ts"
    }
  },
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit",
    "lint": "tsc --noEmit"
  },
  "devDependencies": {
    "@pashki/db": "workspace:^",
    "@supabase/supabase-js": "^2.112.3",
    "@types/node": "^26.2.0",
    "typescript": "^5.6.3",
    "vitest": "^2.1.8"
  }
}
