{
  "name": "@pashki/import",
  "version": "0.1.0",
  "private": true,
  "description": "Server-side recipe import. Deterministic tiers only: structured recipe data, then microdata and plugin markup.",
  "type": "module",
  "exports": {
    ".": {
      "types": "./src/index.ts",
      "default": "./src/index.ts"
    },
    "./job-queue": {
      "types": "./src/supabase-job-queue.ts",
      "default": "./src/supabase-job-queue.ts"
    },
    "./photo-storage": {
      "types": "./src/photo-storage.ts",
      "default": "./src/photo-storage.ts"
    },
    "./sharp": {
      "types": "./src/sharp-preparer.ts",
      "default": "./src/sharp-preparer.ts"
    },
    "./supabase": {
      "types": "./src/supabase-cache.ts",
      "default": "./src/supabase-cache.ts"
    },
    "./photo-bucket": {
      "types": "./src/photo-bucket.ts",
      "default": "./src/photo-bucket.ts"
    }
  },
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit",
    "lint": "tsc --noEmit",
    "eval": "tsx eval.mts"
  },
  "devDependencies": {
    "@pashki/db": "workspace:^",
    "@pashki/platform-client": "workspace:^",
    "@supabase/supabase-js": "^2.112.3",
    "@types/node": "^26.2.0",
    "typescript": "^5.6.3",
    "vitest": "^2.1.8",
    "tsx": "^4.19.2"
  },
  "dependencies": {
    "@pashki/core": "workspace:^",
    "sharp": "^0.35.3"
  }
}
