# Screenshots — not yet received

Five reel screenshots were described but none arrived with the URL and caption
paste of 15 Aug 2026. Nothing is here yet.

Naming when they do arrive, per the intake README: `reel-<dish>-01.png`, numbered
where one recipe spans several frames, so the fixture can list the extra frames
in `extraImagePaths` and fusion across frames is measurable.
